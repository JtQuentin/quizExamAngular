import { LettreReponseDirective } from './lettre-reponse.directive';

describe('LettreReponseDirective', () => {
  it('should create an instance', () => {
    const directive = new LettreReponseDirective();
    expect(directive).toBeTruthy();
  });
});
